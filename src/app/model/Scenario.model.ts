export interface Scenario {
  id?: number;
  name: string;
  description?: string;
  location?: string;
  surface_type?: string;
  available_schedule?: string;
  ability?: string;
  status: string;
}
